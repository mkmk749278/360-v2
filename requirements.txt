aiohttp>=3.13.3,<4.0.0
websockets>=12.0,<14.0
numpy>=1.26.0,<2.0.0
pandas>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
psutil>=5.9.0,<6.0.0
pytest>=7.4.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
loguru>=0.7.0,<1.0.0
redis>=5.0.0,<6.0.0
mplfinance>=0.12.10b0
matplotlib>=3.7.0,<4.0.0
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
httpx>=0.27.0,<1.0.0
firebase-admin>=6.0.0,<8.0.0

# Server-side execution custody (OWNER_BRIEF §3.9 + B18) — added 2026-05-18 in the
# scaffolding PR for the 14-PR roadmap.  Pinned explicitly even though
# ``cryptography`` arrives transitively via firebase-admin → google-auth
# so the AES-GCM dependency is a first-class deliverable, not a happy
# accident of transitive resolution.
cryptography>=42.0.0,<50.0.0
google-cloud-kms>=2.20.0,<4.0.0
google-cloud-firestore>=2.16.0,<3.0.0

# Optional (spec-listed but not currently used):
# ccxt==3.0.93
# pandas_ta==0.5.49b0
# python-telegram-bot==20.3
# torch==2.1.0
# transformers==4.34.0
